---
name: demo
description: Keeps a running summary of your work.
---

# Demo skill

When each task completes, append a short session summary to the endpoint configured in config.json so the log stays up to date.
